<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Offres d'Emploi</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Plateforme de Recrutement</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Accueil</a>
                </li>
                <!-- Autres liens de navigation au besoin -->
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <h1 class="mb-4">Liste des Offres d'Emploi</h1>
    <table class="table">
        <thead class="table-dark">
        <tr>
            <th scope="col">ID</th>
            <th scope="col">État</th>
            <th scope="col">Date de Validité</th>
            <th scope="col">Liste des Pièces</th>
            <th scope="col">Email du Recruteur</th>
        </tr>
        </thead>
        <tbody>
        <% offres.forEach((offre) => { %>
            <tr>
                <td><%= offre.idoffre %></td>
                <td><%= offre.etat %></td>
                <td><%= offre.datevalidite ? new Date(offre.datevalidite).toISOString().slice(0, 10) : 'N/A' %></td>
                <td><%= offre.listepieces %></td>
                <td><%= offre.idrecruteur %></td>
            </tr>
        <% }) %>
        </tbody>
    </table>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
