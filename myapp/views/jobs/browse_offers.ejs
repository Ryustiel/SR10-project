<!DOCTYPE html>
<html lang="fr">
<head>
    <%- include('../head.ejs') %>
    <title>Offres d'Emploi</title>
</head>
<body class="tw-bg-gray-100 tw-font-sans tw-leading-normal tw-tracking-normal">
<%- include('../navbar.ejs') %>
<%- include('../message.ejs') %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h1 class="tw-mb-4 tw-text-4xl tw-font-bold tw-text-gray-800">Parcourir les Offres</h1>
            <!-- Include the search form partial view -->
            <%- include('../search_pagination', {
            action: '/jobs/browse_offers',
            placeholder: 'Rechercher par intitulé',
            search: search
            }) %>
            <%- include('../listings/browse_offers.ejs', {
            offres,
            search,
            sort,
            selectedTypeMetier,
            minSalaire,
            maxSalaire,
            currentPage,
            totalPages,
            typesMetier,
            user,
            userRole
            }) %>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
