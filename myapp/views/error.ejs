<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erreur</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-5">
    <h1><%= message ? message : "Erreur non spécifiée" %></h1>
    <h2><%= (typeof error !== 'undefined' && error.status) ? error.status : "Non disponible" %></h2>
    <pre><%= (typeof error !== 'undefined' && error.stack) ? error.stack : "Pas de stacktrace disponible" %></pre>
</div>
</body>
</html>