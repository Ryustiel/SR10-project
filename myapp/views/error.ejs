<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Erreur</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h1>Erreur</h1>
                </div>
                <div class="card-body">
                    <h2><%= message ? message : "Erreur non spécifiée" %></h2>
                    <h4>Status: <%= error && error.status ? error.status : "Non disponible" %></h4>
                    <pre class="bg-light p-3"><%= error && error.stack ? error.stack : "Pas de stacktrace disponible" %></pre>
                    <p>
                        Une erreur est survenue.
                        <br>
                        Veuillez contacter l'administrateur du site si le problème persiste.
                    </p>
                    <a href="/" class="btn btn-primary">Retour à l'accueil</a>
                </div>
            </div>
        </div>
    </div>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
